# Microsoft sign-in is handled entirely by the backend (OAuth auth-code flow
# with a session cookie), so the frontend needs no Azure configuration.
# All /api requests are proxied to the backend via vite.config.ts.
